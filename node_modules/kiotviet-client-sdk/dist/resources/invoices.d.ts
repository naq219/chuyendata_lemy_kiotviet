import { KiotVietClient } from '../client';
import { Invoice, InvoiceCreateParams, InvoiceUpdateParams, InvoiceListParams, InvoiceListResponse } from '../types';
export declare class InvoiceHandler {
    private client;
    constructor(client: KiotVietClient);
    /**
     * List invoices with optional filtering
     * @param params Filter parameters
     */
    list(params?: InvoiceListParams): Promise<InvoiceListResponse>;
    /**
     * Get an invoice by its ID
     * @param invoiceId The ID of the invoice to retrieve
     */
    getById(invoiceId: number): Promise<Invoice>;
    /**
     * Create a new invoice
     * @param invoiceData The invoice data to create
     */
    create(invoiceData: InvoiceCreateParams): Promise<Invoice>;
    /**
     * Update an existing invoice
     * @param invoiceId The ID of the invoice to update
     * @param invoiceData The invoice data to update
     */
    update(invoiceId: number, invoiceData: Partial<InvoiceUpdateParams>): Promise<Invoice>;
    /**
     * Cancel an invoice
     * @param invoiceId The ID of the invoice to cancel
     * @param reason Optional cancellation reason
     */
    cancel(invoiceId: number, reason?: string): Promise<Invoice>;
    /**
     * Get invoices by date range
     * @param fromDate Start date (YYYY-MM-DD)
     * @param toDate End date (YYYY-MM-DD)
     * @param params Additional filter parameters
     */
    getByDateRange(fromDate: string, toDate: string, params?: Omit<InvoiceListParams, 'fromPurchaseDate' | 'toPurchaseDate'>): Promise<InvoiceListResponse>;
    /**
     * Get invoices by customer
     * @param customerIdentifier Customer's phone number or code
     * @param params Additional filter parameters
     */
    getByCustomer(customerIdentifier: string, params?: Omit<InvoiceListParams, 'customerPhone' | 'customerCode'>): Promise<InvoiceListResponse>;
    /**
     * Get invoices by order
     * @param orderId The ID of the order
     * @param params Additional filter parameters
     */
    getByOrder(orderId: number, params?: Omit<InvoiceListParams, 'orderId'>): Promise<InvoiceListResponse>;
    /**
     * Get an invoice by its code
     * @param code The code of the invoice to retrieve
     */
    getByCode(code: string): Promise<Invoice>;
    /**
     * Delete/void an invoice
     * @param invoiceId The ID of the invoice to delete
     * @param isVoidPayment Whether to void the associated payment
     */
    delete(invoiceId: number, isVoidPayment?: boolean): Promise<void>;
}
