import { KiotVietClient } from '../client';
import { Category, CategoryCreateParams, CategoryUpdateParams, CategoryListParams, CategoryListResponse } from '../types';
export declare class CategoryHandler {
    private client;
    constructor(client: KiotVietClient);
    /**
     * List categories with optional filtering and pagination
     * @param params Filter and pagination parameters
     */
    list(params?: CategoryListParams): Promise<CategoryListResponse>;
    /**
     * Get a category by its ID
     * @param categoryId The ID of the category to retrieve
     */
    getById(categoryId: number): Promise<Category>;
    /**
     * Create a new category
     * @param categoryData The category data to create
     */
    create(categoryData: CategoryCreateParams): Promise<Category>;
    /**
     * Update an existing category
     * @param categoryId The ID of the category to update
     * @param categoryData The category data to update
     */
    update(categoryId: number, categoryData: Partial<CategoryUpdateParams>): Promise<Category>;
    /**
     * Delete a category
     * @param categoryId The ID of the category to delete
     */
    delete(categoryId: number): Promise<void>;
    /**
     * Get categories with hierarchical structure
     * @param params Filter and pagination parameters
     */
    getHierarchical(params?: Omit<CategoryListParams, 'hierarchicalData'>): Promise<CategoryListResponse>;
}
