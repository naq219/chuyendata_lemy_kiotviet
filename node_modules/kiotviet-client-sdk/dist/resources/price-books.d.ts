import { KiotVietClient } from '../client';
import { PriceBookListParams, PriceBookListResponse, PriceBookDetailListResponse, PriceBookDetailUpdateParams } from '../types/price-book';
export declare class PriceBookHandler {
    private client;
    constructor(client: KiotVietClient);
    /**
     * List price books with optional filtering and pagination
     * @param params Filter and pagination parameters
     */
    list(params?: PriceBookListParams): Promise<PriceBookListResponse>;
    /**
     * Get price book details by ID
     * @param priceBookId The ID of the price book to retrieve
     */
    getById(priceBookId: number): Promise<PriceBookDetailListResponse>;
    /**
     * Update price details for a product in a price book
     * @param data The price detail update data
     */
    updatePriceDetail(data: PriceBookDetailUpdateParams): Promise<void>;
}
