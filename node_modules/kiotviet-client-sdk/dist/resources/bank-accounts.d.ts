import { KiotVietClient } from '../client';
import { BankAccount, BankAccountListParams, BankAccountListResponse } from '../types';
export declare class BankAccountHandler {
    private client;
    constructor(client: KiotVietClient);
    /**
     * List bank accounts with optional filtering and pagination
     * @param params Filter and pagination parameters
     */
    list(params?: BankAccountListParams): Promise<BankAccountListResponse>;
    /**
     * Get a bank account by its ID
     * @param accountId The ID of the bank account to retrieve
     */
    getById(accountId: number): Promise<BankAccount>;
}
