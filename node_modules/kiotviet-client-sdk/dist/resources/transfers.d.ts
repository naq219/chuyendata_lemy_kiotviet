import { KiotVietClient } from '../client';
import { Transfer, TransferListParams, TransferListResponse, CreateTransferParams } from '../types/transfer';
export declare class TransferHandler {
    private client;
    constructor(client: KiotVietClient);
    /**
     * Get list of transfers with optional filtering and pagination
     * @param params Query parameters
     */
    list(params?: TransferListParams): Promise<TransferListResponse>;
    /**
     * Get transfer by ID
     * @param id Transfer ID
     */
    getById(id: number): Promise<Transfer>;
    /**
     * Create a new transfer
     * @param data Transfer creation data
     */
    create(data: CreateTransferParams): Promise<Transfer>;
    /**
     * Update transfer by ID
     * @param id Transfer ID
     * @param data Transfer update data
     */
    update(id: number, data: CreateTransferParams): Promise<Transfer>;
    /**
     * Delete transfer by ID
     * @param id Transfer ID
     */
    delete(id: number): Promise<void>;
}
